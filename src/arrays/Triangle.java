package arrays;

public class Triangle {
	@Override
	void collectdata() {
		System.out.println("enter the base of a tringle:");
		int base = sc.nextInt();
		System.out.println("enter the height of a tringle:");
		int height = sc.nextInt();
	}
	@Override
	void calculatedata(int base,int height) {
		area =pi*(radius*radius);
	}
}

}
